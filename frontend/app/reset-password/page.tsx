"use client";
import {useSearchParams,useRouter} from "next/navigation"; import {useState} from "react"; import {authApi,ApiError} from "@/lib/api-client";
export default function ResetPassword(){const search=useSearchParams(),router=useRouter();const token=search.get("token")||"";const [next,setNext]=useState("");const [confirm,setConfirm]=useState("");const [error,setError]=useState("");const [ok,setOk]=useState(false);const [busy,setBusy]=useState(false);async function submit(e:React.FormEvent){e.preventDefault();setError("");if(next.length<10||next.length>128||!/[A-Z]/.test(next)||!/[a-z]/.test(next)||!/[0-9]/.test(next)||!/[^A-Za-z0-9]/.test(next)){setError("Password must be 10-128 characters and include uppercase, lowercase, digit and special character");return;}if(next!==confirm){setError("Passwords do not match");return;}setBusy(true);try{await authApi.resetPassword(token,next);setOk(true);setTimeout(()=>router.push("/login"),1200);}catch(e){setError(e instanceof ApiError?e.message:"Reset link is invalid or expired");}finally{setBusy(false);}}return <main className="page-shell" style={{minHeight:"100vh",display:"grid",placeItems:"center"}}><section className="card" style={{maxWidth:460,width:"100%"}}><div className="eyebrow">ACCOUNT RECOVERY</div><h1 className="page-title">Create a new password</h1>{ok?<div className="alert">Password reset successfully. Returning to sign in…</div>:<form className="form-grid" onSubmit={submit}><div className="field"><label>New password</label><input type="password" value={next} onChange={e=>setNext(e.target.value)} required /></div><div className="field"><label>Confirm password</label><input type="password" value={confirm} onChange={e=>setConfirm(e.target.value)} required /></div>{error&&<div className="alert alert-error">{error}</div>}<button className="btn btn-primary" disabled={busy||!token}>{busy?"Resetting…":"Reset password"}</button></form>}</section></main>}
