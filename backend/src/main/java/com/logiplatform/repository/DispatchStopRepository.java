package com.logiplatform.repository; import com.logiplatform.model.DispatchStop; import org.springframework.data.jpa.repository.JpaRepository; import java.util.*; public interface DispatchStopRepository extends JpaRepository<DispatchStop,UUID>{List<DispatchStop> findAllByTenantIdAndTripIdOrderBySequenceNoAsc(UUID t,UUID trip); Optional<DispatchStop> findByIdAndTenantId(UUID id,UUID t); boolean existsByTenantIdAndTripIdAndSequenceNo(UUID t,UUID trip,int seq);}