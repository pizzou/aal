"use client";
import {useState} from "react"; import {authApi,ApiError} from "@/lib/api-client";
export default function ForgotPassword(){const [email,setEmail]=useState("");const [sent,setSent]=useState(false);const [error,setError]=useState("");const [busy,setBusy]=useState(false);async function submit(e:React.FormEvent){e.preventDefault();setBusy(true);setError("");try{await authApi.forgotPassword(email);setSent(true);}catch(e){setError(e instanceof ApiError?e.message:"Unable to process request");}finally{setBusy(false);}}return <main className="page-shell" style={{minHeight:"100vh",display:"grid",placeItems:"center"}}><section className="card" style={{maxWidth:460,width:"100%"}}><div className="eyebrow">ACCOUNT RECOVERY</div><h1 className="page-title">Reset your password</h1><p className="page-subtitle">Enter your AAL account email. If the account exists, a secure reset link will be sent.</p>{sent?<div className="alert">Check your email for the password reset link.</div>:<form className="form-grid" onSubmit={submit}><div className="field"><label>Email</label><input type="email" value={email} onChange={e=>setEmail(e.target.value)} required /></div>{error&&<div className="alert alert-error">{error}</div>}<button className="btn btn-primary" disabled={busy}>{busy?"Sending…":"Send reset link"}</button></form>}<a href="/login" className="page-subtitle">Back to sign in</a></section></main>}
