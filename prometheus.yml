global:
  scrape_interval: 15s
evaluation_interval: 15s
rule_files:
  - /etc/prometheus/aal-alerts.yml
scrape_configs:
  - job_name: aal-backend
    metrics_path: /actuator/prometheus
    static_configs:
      - targets: [backend:8080]
